@page "/Classifiers/ReportingMethod/Index"

@inject IClassifierRepository<ReportingMethod> classifierRepository

<ClassifierTable Classifiers="@classifiers" ClassifierName="ReportingMethod"></ClassifierTable>

@code {
    List<ReportingMethod> classifiers;

    protected override async Task OnInitializedAsync()
    {
        classifiers = await Task.Run(() => classifierRepository.GetAll());
    }

}
