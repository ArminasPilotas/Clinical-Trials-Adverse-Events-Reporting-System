@page "/Classifiers/ReportingType/Index"

@attribute [Authorize(Policy = "SystemAuthorization", Roles = "Administrator")]

@inject IClassifierRepository<ReportingType> classifierRepository

<ClassifierTable Classifiers="@classifiers" ClassifierName="ReportingType"></ClassifierTable>

@code {
    List<ReportingType> classifiers;

    protected override async Task OnInitializedAsync()
    {
        classifiers = await Task.Run(() => classifierRepository.GetAll());
    }

}
